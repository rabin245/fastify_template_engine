<div class="post-page">
  <h1>Posts</h1>
  <div class="posts">
    <% posts.map(post=> { %>
      <div class="card">
        <h3>
          <%= post.title %>
        </h3>
        <p>
          <%= post.content %>
        </p>
      </div>
      <% }); %>
  </div>
</div>